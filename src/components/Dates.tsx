'use client';

/** Stub during merge — full Dates (calendar of past + planned date
 *  nights, attach photos + afterthoughts) lands in a later wave. */
export function Dates(): null {
  return null;
}

'use client';

/** Stub during merge — full MemoryBank (afterthoughts browser: photos
 *  and notes attached to past dates, searchable per-member) lands in a
 *  later wave. */
export function MemoryBank(): null {
  return null;
}

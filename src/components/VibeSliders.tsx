'use client';

/** Stub during merge — full VibeSliders (per-user P/E/S check-in sliders
 *  with warning-zone support menu) lands in a later wave. */
export function VibeSliders({
  defaultCollapsed: _defaultCollapsed,
}: {
  defaultCollapsed?: boolean;
}): null {
  return null;
}

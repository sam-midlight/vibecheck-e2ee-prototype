'use client';

/** Stub during merge — full VibeOracleBanner (empathic narrator line
 *  derived from projected vibe vectors) lands in a later wave. */
export function VibeOracleBanner(): null {
  return null;
}

'use client';

/** Stub during merge — full Wishlist (ranked list of things you'd
 *  love — experiences, gifts, touches — with partner-visible pips)
 *  lands in a later wave. */
export function Wishlist(): null {
  return null;
}

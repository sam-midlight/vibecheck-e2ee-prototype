'use client';

/**
 * NotificationCenter — stub during the vibecheck2 merge.
 *
 * Final form is a bell-icon popover listing actionable unread items
 * (pending votes, wall pins, matched dates, etc.) derived from the
 * projected event stream. Not load-bearing for the shell layout;
 * rendered as a hidden null until the feature lands.
 */
export function NotificationCenter(): null {
  return null;
}

'use client';

/** Stub during merge — full Roulette (spin wheel of intimate prompts /
 *  date-night ideas / gratitude seeds) lands in a later wave. */
export function Roulette(_props: {
  variant?: 'home' | 'date_night';
  dateId?: string;
} = {}): null {
  return null;
}

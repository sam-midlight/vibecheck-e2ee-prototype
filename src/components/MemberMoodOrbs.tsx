'use client';

/** Stub during merge — full MemberMoodOrbs (breathing orbs above the
 *  Oracle, one per member, hue by their personal vibe tier) lands in a
 *  later wave. */
export function MemberMoodOrbs(): null {
  return null;
}

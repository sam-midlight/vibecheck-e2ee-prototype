'use client';

/** Stub during merge — full HomeworkBanner (intention-setting reducer
 *  with current intention, per-member follow-through pips) lands in a
 *  later wave. */
export function HomeworkBanner(): null {
  return null;
}

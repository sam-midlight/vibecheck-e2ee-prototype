'use client';

/** Stub during merge — full ConfettiBurst (emoji particle burst that
 *  self-destroys after animation) lands in a later wave. Accepts the
 *  prop so VibeSliders' celebration trigger compiles. */
export function ConfettiBurst({ emoji: _emoji }: { emoji: string }): null {
  return null;
}

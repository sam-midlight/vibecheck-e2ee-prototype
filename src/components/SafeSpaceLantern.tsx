'use client';

/** Stub during merge — full SafeSpaceLantern (ambient whisper overlay
 *  on the safe-space route: fades in a vibe-line from the oracle feed
 *  while the page is open) lands in a later wave. */
export function SafeSpaceLantern(): null {
  return null;
}

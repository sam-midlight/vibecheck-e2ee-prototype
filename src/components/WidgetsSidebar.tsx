'use client';

/** Stub during merge — full WidgetsSidebar (at-a-glance widgets: top
 *  need, love tank, rituals, action log, zero-knowledge card) lands in
 *  a later wave. */
export function WidgetsSidebar(): null {
  return null;
}

'use client';

/** Stub during merge — full Gratitude (gratitude-send feed, tap-to-thank
 *  balance view, heart economy) lands in a later wave. */
export function Gratitude(): null {
  return null;
}

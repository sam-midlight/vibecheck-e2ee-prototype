'use client';

/**
 * AffectionLayer — stub during the vibecheck2 merge.
 *
 * The full implementation (tap-to-drop kiss / hug / high-five marks
 * anchored in the current viewport) is part of the feature-component
 * wave that comes after the RoomProvider foundation is in place.
 * Kept as a null-returning shim so RoomProvider's unconditional render
 * compiles without pulling the whole feature in yet.
 */
export function AffectionLayer(): null {
  return null;
}

'use client';

/** Stub during merge — full SafeSpace (anonymous-post thread where
 *  partners share vulnerable thoughts without attribution until they
 *  choose to reveal) lands in a later wave. */
export function SafeSpace(_props: {
  autoOpenPostForm?: boolean;
}): null {
  return null;
}

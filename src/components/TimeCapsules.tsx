'use client';

/** Stub during merge — full TimeCapsules (write-now, unlock-later
 *  messages with scheduled reveal) lands in a later wave. */
export function TimeCapsules(): null {
  return null;
}

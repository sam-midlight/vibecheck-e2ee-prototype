'use client';

/** Stub during merge — full DateVault (single-date detail page: state
 *  reducer over propose/vote/schedule/afterthought events, photo
 *  attachments, shared reflection) lands in a later wave. */
export function DateVault(_props: { dateId: string }): null {
  return null;
}

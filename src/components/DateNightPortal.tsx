'use client';

/** Stub during merge — full DateNightPortal (multi-date countdown wall:
 *  upcoming scheduled + match-without-schedule clusters with quick
 *  RSVP / reschedule / archive actions) lands in a later wave. */
export function DateNightPortal(): null {
  return null;
}

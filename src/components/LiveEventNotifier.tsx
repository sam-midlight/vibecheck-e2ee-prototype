'use client';

/**
 * LiveEventNotifier — stub during the vibecheck2 merge.
 *
 * Final form watches `events` from RoomProvider and fires deep-linked
 * toasts ("💖 You matched on {title}") with Open buttons into the
 * relevant feature surface. Rendered unconditionally from RoomProvider
 * so the foundation can land before the feature is ported.
 */
export function LiveEventNotifier(): null {
  return null;
}

'use client';

/** Stub during merge — full DateGeneratorWidget (idea generator that
 *  suggests date nights from the shared preference pool) lands in a
 *  later wave. */
export function DateGeneratorWidget(): null {
  return null;
}

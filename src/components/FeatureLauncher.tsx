'use client';

/** Stub during merge — full FeatureLauncher (fixed-position floating
 *  button fanning out into the full sheet grid: Dates, Safe Space,
 *  Mind Reader, Wishlist, Time Capsules, Gratitude) lands in a later
 *  wave. */
export function FeatureLauncher(): null {
  return null;
}
